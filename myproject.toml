[build-system]
requires = ["maturin>=0.14,<0.15"]
build-backend = "maturin"

[project]
name = "vllm-rs"
description = "A blazing-fast ⚡, lightweight Rust 🦀 implementation of vLLM."
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
    "Programming Language :: Python :: Implementation :: PyPy",
]

[tool.maturin]
python-source = "python"
include = ["*.pyi", { path = "target/release/runner" }]
sdist-include = ["PyPi.md"]